# docker-compose.prod.yml
# 전체 AUTOBIOGRAPHY CICD 스택 (프로덕션 환경)  # ← 🔄 프로젝트명 변경

# version: '3.8'

services:
  # 백엔드 서비스 (Spring Boot)
  backend:
    image: autobiography-backend:${BACKEND_IMAGE_TAG:-latest}
    container_name: autobiography-backend  # ← 🔄 -new 제거
    environment:
      # Spring 프로파일
      SPRING_PROFILES_ACTIVE: prod
      
      # 데이터베이스 연결
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/autobiography_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useUnicode=true  # ← 🔄 DB명 변경
      SPRING_DATASOURCE_USERNAME: ssafy  # ← 🔄 사용자명 변경
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}  # ← 🔄 비밀번호 변경
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver
      
      # JPA 설정
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: false
      SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.MySQL8Dialect
      SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT: org.hibernate.dialect.MySQL8Dialect
      
      # Redis 연결
      SPRING_DATA_REDIS_HOST: redis-server
      SPRING_DATA_REDIS_PORT: 6379
      SPRING_DATA_REDIS_TIMEOUT: 2000ms
      # SPRING_REDIS_PASSWORD: ""  # ← Redis 비밀번호 없음
      SPRING_DATA_REDIS_DATABASE: 0
      
      # 서버 설정
      SERVER_PORT: 8081
      
      # 로깅 설정
      LOGGING_LEVEL_COM_SSAFY: INFO
      LOGGING_LEVEL_ROOT: WARN
      
      # Jenkins에서 주입받을 민감 정보들
      AWS_S3_ACCESS_KEY : ${AWS_S3_ACCESS_KEY}
      AWS_S3_SECRET_KEY : ${AWS_S3_SECRET_KEY}
      DB_PASSWORD : ${DB_PASSWORD}
      JWT_SECRET : ${JWT_SECRET}
      GOOGLE_CLIENT_ID : ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET : ${GOOGLE_CLIENT_SECRET}
      MAIL_USERNAME : ${MAIL_USERNAME}
      MAIL_PASSWORD : ${MAIL_PASSWORD}
      CLOVA_STT_API_KEY : ${CLOVA_STT_API_KEY}
      OPENAI_API_KEY : ${OPENAI_API_KEY}
      LIVEKIT_API_KEY : ${LIVEKIT_API_KEY}
      LIVEKIT_API_SECRET : ${LIVEKIT_API_SECRET}
    ports:
      - "8081:8081"
    depends_on:
      mysql:
        condition: service_healthy  # ← 🔄 헬스체크 조건 추가
      redis:
        condition: service_started  # ← 🔄 헬스체크 조건 추가
    restart: unless-stopped
    networks:
      - app-network  # ← 🔄 네트워크명 변경
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 30s
      retries: 3
      start_period: 120s

  # 프론트엔드 서비스 (Vue.js + Nginx)
  frontend:
    image: autobiography-frontend:${FRONTEND_IMAGE_TAG:-latest}
    container_name: autobiography-frontend  # ← 🔄 -new 제거
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network  # ← 🔄 네트워크명 변경
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  # MySQL 데이터베이스
  mysql:
    image: mysql:8.0.37  # ← 🔄 버전 변경 (8.0 → 8.0.37)
    container_name: mysql-db  # ← 🔄 컨테이너명 변경
    environment:
      MYSQL_ROOT_PASSWORD: 959697989900  # ← 🔄 루트 비밀번호 변경
      MYSQL_DATABASE: autobiography_db  # ← 🔄 데이터베이스명 변경
      MYSQL_USER: ssafy  # ← 🔄 사용자명 변경
      MYSQL_PASSWORD: 959697989900  # ← 🔄 사용자 비밀번호 변경
      MYSQL_ROOT_HOST: '%'
    ports:
      - "3307:3306"  # ← 🔄 포트 변경 (13306 → 3307)
    volumes:
      - mysql-data:/var/lib/mysql  # ← 🔄 볼륨명 변경
      - ./infrastructure/mysql/init:/docker-entrypoint-initdb.d
      - ./infrastructure/mysql/conf:/etc/mysql/conf.d
    restart: always  # ← 🔄 재시작 정책 변경 (unless-stopped → always)
    command: 
      - --default-authentication-plugin=mysql_native_password  # ← 🔄 단순화 (복사본과 일치)
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --innodb-buffer-pool-size=256M
    networks:
      - app-network  # ← 🔄 네트워크명 변경
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "ssafy", "-p959697989900"]  # ← 🔄 사용자 정보 변경
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # Redis 캐시
  redis:
    image: redis:7.2-alpine  # ← 🔄 버전 변경 (7-alpine → 7.2-alpine)
    container_name: redis-server  # ← 🔄 컨테이너명 변경
    command: 
      - redis-server  # ← 🔄 비밀번호 제거 (복사본에 없음)
      - --appendonly 
      - "yes"
      - --appendfsync
      - everysec
      - --maxmemory 
      - 128mb
      - --maxmemory-policy 
      - allkeys-lru
      - --replica-read-only
      - "no"
      - --save
      - ""   
    ports:
      - "6379:6379"  # ← 🔄 포트 변경 (16379 → 6379)
    volumes:
      - redis_data:/data
    restart: always  # ← 🔄 재시작 정책 변경 (unless-stopped → always)
    networks:
      - app-network  # ← 🔄 네트워크명 변경
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]  # ← 🔄 비밀번호 제거
      interval: 30s
      timeout: 5s
      retries: 3

  # 데이터베이스 관리 도구 (개발/관리용)
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: autobiography-phpmyadmin  # ← 🔄 컨테이너명 변경
    environment:
      PMA_HOST: mysql
      PMA_USER: ssafy  # ← 🔄 사용자명 변경
      PMA_PASSWORD: 959697989900  # ← 🔄 비밀번호 변경
      PMA_ABSOLUTE_URI: http://i13c203.p.ssafy.io:8082/
    ports:
      - "8082:80"
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network  # ← 🔄 네트워크명 변경

volumes:
  mysql-data:  # ← 🔄 볼륨명 변경 (mysql_data → mysql-data)
    driver: local
  redis_data:
    driver: local

networks:
  app-network:  # ← 🔄 네트워크명 변경 (ssafy-network → app-network)
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.0.0/16